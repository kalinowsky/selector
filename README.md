# Selector App

## Project based on Vite

Used React + konva / react-konva libraries

#### Recommended

- node 16+
- pnpm 8.6.10+

#### Commands:

- `pnpm i` - install dependencies
- `pnpm dev` - run dev
